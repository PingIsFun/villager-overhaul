# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true
org.gradle.configuration-cache=false

mod.version=0.1.0
mod.group=io.github.pingisfun
mod.id=villager-rebalance
mod.name=Villager Rebalance
mod.description=Villager Rebalance is a server-side Fabric mod that makes villager progression harder to exploit while keeping the mechanics configurable. It targets Minecraft 26.1 and does not require a client-side install.

deps.fabric_loader=0.19.2

# Replace with your Modrinth project slug or ID before publishing.
modrinth.id=villager-rebalance
